
name: flutter_client
description: Avatar anime IA (web) con Rasa + STT/TTS
publish_to: "none"
environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_tts: ^3.8.2
  speech_to_text: ^6.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/avatar_idle.png
    - assets/avatar_talk.png
    - assets/emociones/
